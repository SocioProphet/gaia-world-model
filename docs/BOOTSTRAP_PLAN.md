# Bootstrap plan

This repository is the canonical model spine.

- Core contracts live under `gaia/core`.
- Profile projections live under `gaia/profiles`.
- Domain packs live under `gaia/domains`.
- Schema materializations live under `schemas`.
- Minimal executable examples live under `examples`.

`orion-field-intelligence` remains the private incubator and applied reference surface.
